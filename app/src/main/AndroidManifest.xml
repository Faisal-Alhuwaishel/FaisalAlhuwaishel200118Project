<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.faisalalhuwaishel200118project">
    <!-- Request Internet connection -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- Request to check Internet connection state -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FaisalAlhuwaishel200118Project"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".sqlEdit"
            android:exported="false" />
        <activity
            android:name=".sqlList"
            android:exported="false" />
        <activity
            android:name=".WeatherEdit"
            android:exported="false" />
        <activity
            android:name=".FirebaseInsert"
            android:exported="false" />
        <activity
            android:name=".FirebaseEdit"
            android:exported="false" />
        <activity
            android:name=".FirebaseList"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>